export type Genders = {
  cat?: boolean;
  dog?: boolean;
  bunny?: boolean;
  fake?: boolean;
  female?: boolean;
  male?: boolean;
  surname?: boolean;
};

export const names: Record<string, Genders> = {};
